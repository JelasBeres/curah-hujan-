version: '3.8'

services:
  ml-service:
    build:
      context: ./apps/ml-service
      dockerfile: Dockerfile
    ports:
      - "8001:8000"
    environment:
      - PYTHONUNBUFFERED=1
    env_file:
      - ./apps/ml-service/.env
    restart: unless-stopped
